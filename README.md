# official-site
新潟大学マイクラ同好会のWEBサイト

# License
This project is licensed unser the Apache License, Version 2.0 - see the LICENSE file for details.
However, the following directories have different licenses:
- Files in `content/`, `static/` and `assets/images` directory are licensed under the CC BY-SA 4.0 Universal license.
- Files in `public/` directory are licensed under the CC BY-SA 4.0 Universal license(please read description below).

## License in `public/` directory
`public/` directory has been added to the repository mistakenly. It has already beedn detached but old commits may contain `public/` directory.
So, those who use new commit in which `public/` directory detached do not have to read this section.
Files in `public/` are built one using files in `content/`, `static/` and `assets/images`. So, most files in `public/` are licensed CC BY-SA 4.0.
However, `public/` directory may contain files which are not derived from files licensed under CC BY-SA 4.0(ex. JavaScript files). Such files are licensed under Apache License.
For that reson, files in `public/` directory are very complicated on license. So, to use files in `public/` is **not** recommended.
